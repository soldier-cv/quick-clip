<Window x:Class="QuickClip.Views.FontPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="选择字体"
        Width="360"
        Height="480"
        MinWidth="320"
        MinHeight="380"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResize"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="{DynamicResource Theme.Panel}"
        BorderBrush="{DynamicResource Theme.Border}"
        BorderThickness="1"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">

    <Window.Resources>
        <Style x:Key="FontPickerListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text}" />
            <Setter Property="Height" Value="36" />
            <Setter Property="Margin" Value="0,1" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Border x:Name="Indicator"
                                        Width="3"
                                        Height="16"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        CornerRadius="1.5"
                                        Background="{DynamicResource Theme.Accent}"
                                        Visibility="Collapsed" />
                                <ContentPresenter Margin="6,0,0,0" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.CardHover}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.CardSelected}" />
                                <Setter TargetName="Indicator" Property="Visibility" Value="Visible" />
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Accent}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="38"
                            ResizeBorderThickness="6"
                            GlassFrameThickness="0"
                            CornerRadius="8"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Grid Background="{DynamicResource Theme.Panel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="38" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 自绘标题栏 -->
        <Grid Grid.Row="0" Background="{DynamicResource Theme.Panel}">
            <TextBlock Text="选择字体"
                       Margin="16,0,0,0"
                       VerticalAlignment="Center"
                       FontSize="13"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource Theme.Text}" />
            <Button HorizontalAlignment="Right"
                    VerticalAlignment="Stretch"
                    Width="42"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="OnCloseClicked"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True"
                    ToolTip="关闭">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="Transparent">
                            <TextBlock Text="&#xE8BB;"
                                       FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                       FontSize="10"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource Theme.TextSecondary}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#22FF4444" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>

        <!-- 搜索过滤框 -->
        <Grid Grid.Row="1" Margin="14,4,14,8">
            <ui:TextBox x:Name="SearchBox"
                        Height="34"
                        PlaceholderText="搜索本机字体（名称 / 拼音）…"
                        TextChanged="OnSearchTextChanged"
                        VerticalContentAlignment="Center"
                        Padding="10,0" />
        </Grid>

        <!-- 字体列表 -->
        <ListBox x:Name="FontListBox"
                 Grid.Row="2"
                 Margin="14,0,14,8"
                 Background="{DynamicResource Theme.Card}"
                 BorderBrush="{DynamicResource Theme.Border}"
                 BorderThickness="1"
                 Padding="4"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ItemContainerStyle="{StaticResource FontPickerListBoxItemStyle}"
                 MouseDoubleClick="OnFontListBoxDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"
                               FontSize="13"
                               VerticalAlignment="Center" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- 底栏操作区 -->
        <Border Grid.Row="3"
                Padding="14,10,14,14"
                Background="{DynamicResource Theme.Panel}"
                BorderBrush="{DynamicResource Theme.Border}"
                BorderThickness="0,1,0,0">
            <Grid>
                <ui:Button x:Name="BrowseFileButton"
                           Appearance="Secondary"
                           Height="32"
                           Padding="10,0"
                           HorizontalAlignment="Left"
                           Content="从文件选择…"
                           ToolTip="选择外部 .ttf / .otf / .ttc 文件"
                           Click="OnBrowseFileClicked" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <ui:Button Appearance="Secondary"
                               Height="32"
                               Padding="14,0"
                               Content="取消"
                               Click="OnCancelClicked" />
                    <ui:Button x:Name="OkButton"
                               Margin="8,0,0,0"
                               Appearance="Primary"
                               Height="32"
                               Padding="14,0"
                               Content="确定"
                               Click="OnOkClicked" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
